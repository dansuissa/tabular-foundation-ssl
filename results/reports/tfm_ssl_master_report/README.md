# TFM-SSL Master Report (INCOMPLETE)

This directory is a scaffold. Full executive summary / rankings / paired SSL deltas
will be generated only after Phases A–C complete (or failures are fully accounted for).

**Current status:** blocked on `TABPFN_TOKEN` before Phase A launch.

See `run_manifest.json` and `results/validation/tfm_ssl_test_report.md`.
